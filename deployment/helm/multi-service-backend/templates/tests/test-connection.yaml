# This doesn't currently work
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-service-backend.fullname" . }}-test-connection"
  labels:
    {{- include "multi-service-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-service-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
